// Re-export Database type for dashboard use
// Using direct path import to ensure TypeScript resolves the full type
export type { Database } from '../../../../../packages/supabase/src/database.types';
